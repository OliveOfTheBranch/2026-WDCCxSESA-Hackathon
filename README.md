# 2026 WDCC x SESA Hackathon

The repository for the "d1 larpers" team.

#### Using Smartscrollr
Run `npm run dev` to host the web application locally.
Run `npm run devStart` to host the AI integration server locally.
Please note that both terminals running these commands must be live for the application to run correctly.
